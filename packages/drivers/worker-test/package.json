{
	"name": "@template-app/worker-test",
	"version": "0.0.0",
	"dependencies": {
		"@template-app/service-crash-sentry": "^0.0.0"
	},
	"devDependencies": {
		"rimraf": "^3.0.2",
		"wrangler": "^2.6.2"
	},
	"license": "MIT",
	"main": "src/index.js",
	"scripts": {
		"clean": "rimraf build *.log",
		"clean:all": "yarn clean && rimraf node_modules",
		"dev": "wrangler dev",
		"prod": "wrangler publish",
		"prod:preview": "wrangler publish --dry-run --outdir ./build"
	},
	"type": "module"
}
