{
	"name": "@template-app/shared-schema-orbit",
	"version": "0.0.0",
	"dependencies": {
		"@orbit/records": "^0.17.0"
	},
	"devDependencies": {
		"rimraf": "^3.0.2"
	},
	"license": "MIT",
	"main": "src/index.js",
	"scripts": {
		"clean": "rimraf *.log",
		"clean:all": "yarn clean && rimraf node_modules"
	},
	"type": "module"
}
